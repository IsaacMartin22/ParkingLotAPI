# API-Service

Run with `mvn spring-boot:run`

Uses maven. Publish jars with `mvn clean package` and run with `java -jar target/JAR_NAME.jar`

Check local database via ``http://localhost:8080/h2-console``

Endpoints: