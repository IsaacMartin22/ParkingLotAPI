app.environment=staging

server.port=8080

spring.datasource.url=${STAGING_DATASOURCE_URL}
spring.datasource.username=${STAGING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${STAGING_DATASOURCE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=validate

management.metrics.tags.environment=staging

app.contact.to-email=${CONTACT_TO_EMAIL:IsaacMartin151@gmail.com}
app.contact.from-email=${CONTACT_FROM_EMAIL:no-reply@parkinglot.local}
app.contact.subject=${CONTACT_SUBJECT:Parking Lot API Contact Message}
